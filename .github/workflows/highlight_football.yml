name: Highlight Football Update

on:
  schedule:
    - cron: '15 0,12 * * *' # 07:15 และ 19:15 ไทย
  workflow_dispatch:

concurrency:
  group: highlight-football-update-${{ github.ref }}
  cancel-in-progress: true

jobs:
  update_highlight_football:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      # 2️⃣ Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --timeout=100 requests beautifulsoup4 lxml python-telegram-bot dropbox
        env:
          PIP_CACHE_DIR: ~/.cache/pip

      # 4️⃣ Run Highlight Football Scripts
      - name: Run Highlight Football Scripts
        run: |
          python scripts/highlight_football/buaksib_update.py
          python scripts/highlight_football/dbidhl_update.py
          python scripts/highlight_football/dlshl_update.py
          python scripts/highlight_football/hugballhl_update.py

      # 5️⃣ Check for changes
      - name: Check for changes
        id: check-changes
        run: |
          git add data/highlight_football/*.json
          if git diff --staged --quiet; then
            echo "HAS_CHANGES=false" >> $GITHUB_ENV
            echo "NO_CHANGES=true" >> $GITHUB_ENV
            echo "No changes detected"
          else
            echo "HAS_CHANGES=true" >> $GITHUB_ENV
            echo "NO_CHANGES=false" >> $GITHUB_ENV
            echo "Changes detected"
          fi

      # 6️⃣ Send Telegram Notification (Only for manual trigger or schedule)
      - name: Send Telegram Notification
        if: ${{ (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') && env.HAS_CHANGES == 'true' }}
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python - <<EOF
          import os
          import datetime
          import requests

          files = [
              "buaksibhl.json",
              "dbidhl.json", 
              "dlshl.json",
              "hugballhl.json"
          ]

          token = os.environ.get("TELEGRAM_BOT_TOKEN")
          chat_id = os.environ.get("TELEGRAM_CHAT_ID")

          if not token or not chat_id:
              print("❌ Telegram token หรือ chat_id ไม่ถูกต้อง")
              exit(1)

          time_now = datetime.datetime.utcnow() + datetime.timedelta(hours=7)
          time_str = time_now.strftime("%d-%m-%Y %H:%M:%S")

          msg = "📁 อัปโหลดไฮไลท์ฟุตบอลสำเร็จ:\n\n"
          for f in files:
              msg += f"✅ /data/highlight_football/{f}\n"
          msg += f"\n🎉 อัปโหลดทั้งหมดเสร็จสิ้นแล้ว {time_str}"

          r = requests.post(
              f"https://api.telegram.org/bot{token}/sendMessage",
              data={"chat_id": chat_id, "text": msg}
          )

          print(f"Telegram response: {r.status_code} {r.text}")
          if r.status_code != 200:
              print("❌ ส่งข้อความ Telegram ไม่สำเร็จ")
          else:
              print("✅ ส่งข้อความ Telegram สำเร็จ")
          EOF

      # 7️⃣ Commit & Push JSON (Only if changes detected)
      - name: Commit & Push JSON
        if: env.HAS_CHANGES == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add data/highlight_football/*.json
          git commit -m "Auto update Highlight Football"
          git push origin main

      # 8️⃣ Skip notification message
      - name: Skip notification
        if: env.NO_CHANGES == 'true'
        run: |
          echo "No changes detected, skipping Telegram notification and commit"
